<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suzyne.shop.domain.store.dao.StoreDao">
<select id="findAllCount" parameterType="com.suzyne.shop.domain.store.vo.StoreSearch" resultType="int">
select count(store_id) as totalCount
  from store
</select>

<select id="findAll" parameterType="com.suzyne.shop.domain.store.vo.StoreSearch" resultType="com.suzyne.shop.domain.store.vo.Store">
select store_id        as storeId
     , store_nm        as storeNm
     , address         as address
     , city            as city
     , province        as province
     , postal_cd       as postalCd
     , store_open_time as storeOpenTime
     , telephone       as telephone
     , is_use          as isUse
     , reg_date        as regDate
     , reg_person      as regPerson
     , last_mod_date   as lastModDate
     , last_mod_person as lastModPerson
  from store
 order by store_id
offset #{startRownum} rows
 fetch next #{listSize} row only
</select>

<select id="findById" parameterType="com.suzyne.shop.domain.store.vo.StoreSearch" resultType="com.suzyne.shop.domain.store.vo.Store">
select store_id        as storeId
     , store_nm        as storeNm
     , address         as address
     , city            as city
     , province        as province
     , postal_cd       as postalCd
     , store_open_time as storeOpenTime
     , telephone       as telephone
     , is_use          as isUse
     , reg_date        as regDate
     , reg_person      as regPerson
     , last_mod_date   as lastModDate
     , last_mod_person as lastModPerson
  from store
 where store_id = #{item_id}
</select>

</mapper>