<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hannamsm.shop.domain.favourite.dao.FavouriteDao">
<select id="findByAccountIdCount" parameterType="com.hannamsm.shop.domain.favourite.vo.FavouriteItemSearch" resultType="int">
select count(a.item_id) as totalCount
  from favourite a
       inner join item b on b.item_id = a.item_id and is_use = 1
 where a.account_id = #{accountId}
</select>

<select id="findByAccountId" parameterType="com.hannamsm.shop.domain.favourite.vo.FavouriteItemSearch" resultType="com.hannamsm.shop.domain.favourite.vo.FavouriteItem">
select a.item_id        as itemId
     , b.upc             as upc
     , b.item_kr_nm      as itemKrNm
     , b.item_en_nm      as itemEnNm
     , b.item_size       as itemSize
     , b.receiving_price as receiving_price
     , b.regular_price   as regularPrice
     , b.item_tax_cd     as itemTaxCd
     , b.sale_unit       as saleUnit
     , b.item_deposit_cd as itemDepositCd
     , b.category_cd     as categoryCd
     , a.reg_date        as regDate
     , a.reg_person      as regPerson
     , a.last_mod_date   as lastModDate
     , a.last_mod_person as lastModPerson
  from favourite a
       inner join item b on b.item_id = a.item_id and is_use = 1
 where a.account_id = #{accountId}
 order by a.item_id
offset #{startRownum} rows
 fetch next #{listSize} row only
</select>

<insert id="add" parameterType="com.hannamsm.shop.domain.favourite.vo.FavouriteItem">
merge favourite as a
using (select #{accountId}     AS account_id
            , #{itemId}        AS item_id
            , getdate()        AS reg_date
            , #{regPerson}     AS reg_person
            , getdate()        AS last_mod_date
            , #{lastModPerson} AS last_mod_person
      ) as b
        on (     a.account_id = b.account_id
             and a.item_id = b.item_id)
        when not matched then
             insert ( account_id
                    , item_id
                    , reg_date
                    , reg_person
                    , last_mod_date
                    , last_mod_person
             ) values (
                      b.account_id
                    , b.item_id
                    , b.reg_date
                    , b.reg_person
                    , b.last_mod_date
                    , b.last_mod_person
             )
        when matched then
             update set a.last_mod_person = b.last_mod_person
                  , a.last_mod_date = b.last_mod_date;
</insert>
<delete id="delete" parameterType="com.hannamsm.shop.domain.favourite.vo.FavouriteItem">
delete favourite
 where account_id = #{accountId}
   and item_id = #{itemId}
</delete>
</mapper>